import {Link,useNavigate} from "react-router-dom";import {LogOut,ShieldCheck} from "lucide-react";import {useAuth} from "../context/AuthContext";
export default function Navbar(){const{user,logout}=useAuth(),nav=useNavigate();return <nav className="navbar"><Link className="brand" to="/"><ShieldCheck/>Auth<span>Flow</span></Link><div className="navlinks">{user?<><span className="email-chip">{user.email}</span><button className="ghost" onClick={()=>{logout();nav("/login")}}><LogOut size={17}/>Logout</button></>:<><Link to="/login">Login</Link><Link className="nav-cta" to="/signup">Get started</Link></>}</div></nav>}