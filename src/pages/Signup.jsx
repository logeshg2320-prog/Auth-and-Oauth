import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import api from "../api/apiClient";import AuthLayout from "../components/AuthLayout";import {Eye,EyeOff,Loader2} from "lucide-react";
export default function Signup(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[confirm,setConfirm]=useState(""),[show,setShow]=useState(false),[loading,setLoading]=useState(false),[error,setError]=useState("");const nav=useNavigate();
const submit=async e=>{e.preventDefault();setError("");if(password!==confirm)return setError("Passwords do not match.");if(password.length<6)return setError("Password must be at least 6 characters.");setLoading(true);try{await api.post("/signup",{email,password});localStorage.setItem("verification_email",email);nav("/verify-email")}catch(err){setError(err.response?.data?.detail||"Unable to create account.")}finally{setLoading(false)}};
const google=import.meta.env.VITE_GOOGLE_AUTH_URL||"http://127.0.0.1:8000/auth/google";
return <AuthLayout title="Create your account" subtitle="Start your secure authentication journey."><form onSubmit={submit}>{error&&<div className="alert error">{error}</div>}<label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com" required/></label><label>Password<div className="password-field"><input type={show?"text":"password"} value={password} onChange={e=>setPassword(e.target.value)} placeholder="Create a password" required/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff size={18}/>:<Eye size={18}/>}</button></div></label><label>Confirm password<input type={show?"text":"password"} value={confirm} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm your password" required/></label><button className="btn primary full" disabled={loading}>{loading?<><Loader2 className="spin" size={18}/>Creating account...</>:"Create account"}</button></form><div className="divider">or continue with</div><a className="google" href={google}>G Continue with Google</a><p className="switch">Already have an account? <Link to="/login">Sign in</Link></p></AuthLayout>}