import {useState} from "react";import {Link,useNavigate} from "react-router-dom";import api from "../api/apiClient";import AuthLayout from "../components/AuthLayout";
export default function ForgotPassword(){const[email,setEmail]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false),nav=useNavigate();const submit=async e=>{e.preventDefault();setLoading(true);setError("");try{await api.post("/forgot-password",{email});localStorage.setItem("reset_email",email);nav("/reset-password",{state:{message:"If an account exists, a reset code has been sent."}})}catch(err){setError(err.response?.data?.detail||"Unable to process request.")}finally{setLoading(false)}};return <AuthLayout title="Forgot your password?" subtitle="Enter your email and we'll send a secure reset code."><form onSubmit={submit}>{error&&<div className="alert error">{error}</div>}<label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><button className="btn primary full" disabled={loading}>{loading?"Sending code...":"Send reset code"}</button></form><p className="switch"><Link to="/login">Back to sign in</Link></p></AuthLayout>}