import {useState} from "react";import {Link,useLocation,useNavigate} from "react-router-dom";import AuthLayout from "../components/AuthLayout";import {useAuth} from "../context/AuthContext";
export default function Login(){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[error,setError]=useState(""),[loading,setLoading]=useState(false),{login}=useAuth(),nav=useNavigate(),loc=useLocation();const submit=async e=>{e.preventDefault();setError("");setLoading(true);try{await login(email,password);nav("/dashboard")}catch(err){setError(err.response?.data?.detail||"Incorrect email or password.")}finally{setLoading(false)}};const google=import.meta.env.VITE_GOOGLE_AUTH_URL||"http://127.0.0.1:8000/auth/google";
return <AuthLayout title="Welcome back" subtitle="Sign in to continue to your account.">{loc.state?.message&&<div className="alert success">{loc.state.message}</div>}{error&&<div className="alert error">{error}</div>}<form onSubmit={submit}><label>Email address<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required/></label><div className="form-meta"><Link to="/forgot-password">Forgot password?</Link></div><button className="btn primary full" disabled={loading}>{loading?"Signing in...":"Sign in"}</button></form><div className="divider">or continue with</div><a className="google" href={google}>G Continue with Google</a><p className="switch">New here? <Link to="/signup">Create an account</Link></p></AuthLayout>}